```bash
pip freeze > requirements.txt - создаёт папку со всеми библиотеками, которые используются в проекте
pip install -r requirements.txt - устанавливает все библиотеки из папки requirements, используемые в проекте
```
